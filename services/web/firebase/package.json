{
  "name": "web-firebase",
  "version": "1.0.0",
  "description": "The web service's Firebase project.",
  "main": "index.js",
  "scripts": {
    "firebase:login": "firebase login",
    "deploy": "firebase deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Peter Gerwing",
  "license": "ISC",
  "devDependencies": {
    "firebase-tools": "^8.10.0"
  }
}
